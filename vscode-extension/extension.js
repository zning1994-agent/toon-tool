// VS Code Extension Host
// Required even if empty — VS Code needs an entry point

function activate(context) {
  // Extension activates when a .toon file is opened
}

function deactivate() {}

module.exports = { activate, deactivate };
